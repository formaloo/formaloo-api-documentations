Lists active integration app definitions in the public catalog.

Catalog entries describe what Formaloo supports; they are not provider connections or form-level configurations. `is_installed` reports whether the authenticated profile has an installation record (`null` when there is no authenticated installation context). An installed entry can still require OAuth, credentials, or a configured integration item before it can exchange data.
