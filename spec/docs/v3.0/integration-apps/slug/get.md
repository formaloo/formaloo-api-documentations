Retrieves one integration app definition by its catalog slug.

Use the returned `type`, `settings`, and tags to identify the supported capability. This resource is catalog metadata: it does not prove that the current user has authorized the provider or configured a form-level integration.
