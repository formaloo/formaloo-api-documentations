Lists mapping-safe HubSpot properties for `contacts`, `companies`, or `deals`.

Call this before creating or updating a HubSpot form mapping. Store the returned property `name` as `destination_field.name` and its `fieldType` as `destination_field.type`; do not substitute HubSpot's separate storage `type` value or invent property identifiers.
