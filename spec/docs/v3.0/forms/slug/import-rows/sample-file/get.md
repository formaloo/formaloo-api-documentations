Downloads a sample spreadsheet matching the form's fields for row import. Use this before importing rows so the client or user can prepare data in the expected format.

## Behavior

- **Status:** `200`
- **Media type:** `application/vnd.ms-excel` (binary file attachment), **not** JSON
- **Envelope:** none — response is the file itself, not the standard JSON envelope
