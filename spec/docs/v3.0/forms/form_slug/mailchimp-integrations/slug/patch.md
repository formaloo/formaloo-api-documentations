Updates a form-level Mailchimp mapping. Every PATCH must resend the complete `mapped_fields` object, including a saved email field with both `tag: email_address` and `type: email_address`. This applies even to list-only changes; omitting the mapping fails validation. Retrieve the current integration and the audience's merge fields first because the submitted mapping replaces the saved mapping object.
