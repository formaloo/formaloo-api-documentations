Creates a Mailchimp audience mapping for a form.

Connect Mailchimp, list available audiences and merge fields, then pass the selected audience as `list_id`. `mapped_fields` is keyed by saved Formaloo field slug and uses discovered Mailchimp merge-field metadata. At least one source field must map to Mailchimp's email field.
