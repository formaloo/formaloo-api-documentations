Lists NetSuite configurations for the selected form.

These are form-level mappings, separate from the catalog installation and NetSuite credential connection. Mappings are grouped by supported NetSuite record type.
