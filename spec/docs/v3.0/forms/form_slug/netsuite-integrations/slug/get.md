Retrieves one form-level NetSuite mapping by integration slug. Retrieve this resource before updating so unchanged record mappings and provider metadata are preserved.

This confirms saved mapping state, not whether the workspace-scoped NetSuite
connection is healthy.
