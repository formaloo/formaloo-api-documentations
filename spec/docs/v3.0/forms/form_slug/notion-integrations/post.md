Creates a Notion database mapping for a form.

Connect Notion and discover the accessible workspace, database, and database properties first. Pass their stable IDs as `workspace_id` and `database_id`. `mapped_fields` is keyed by saved Formaloo field slug and uses discovered Notion property metadata.
