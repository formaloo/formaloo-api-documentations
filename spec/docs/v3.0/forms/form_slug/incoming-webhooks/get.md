Lists the incoming webhooks configured for a form. Incoming webhooks receive data from external systems and create or update Formaloo rows; they are separate from outbound form webhooks.

Authentication secrets are never returned. A generated secret appears only in the response that created it.

Use the capture endpoint on an inactive webhook before configuring mappings or activating it.
