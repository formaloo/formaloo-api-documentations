Retrieves one incoming webhook configuration.

The response includes its public receive URL, capture state, latest sample payload, mapping, action, and activation status. Authentication secrets remain redacted; only a newly generated secret is returned once in the response that generated it.
