Lists HubSpot configurations for the selected form.

These are form-level mappings, separate from the integration catalog installation and the user's HubSpot connection. Each mapping groups saved Formaloo source field slugs by HubSpot object type and pairs them with discovered destination-property metadata.
