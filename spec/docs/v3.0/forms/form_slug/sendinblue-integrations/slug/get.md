Retrieves one form-level Brevo list mapping by integration slug. The API path
uses the legacy provider name Sendinblue. This confirms saved mapping state,
not whether Brevo authorization is still valid.
