Creates a binding that maps a blueprint `node_key` to a built Formaloo `artifact_slug`. Use this after building or adopting an artifact so later plan iterations can update the same resource instead of creating duplicates.

`node_key` must be unique within the workflow.
