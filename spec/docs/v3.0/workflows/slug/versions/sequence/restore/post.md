Restores a workflow to a previous version sequence. Use this when rolling back blueprint or workflow field changes after a bad iteration.

Optional body field `note` is recorded on the restore version. Returns the restored workflow payload on success.
