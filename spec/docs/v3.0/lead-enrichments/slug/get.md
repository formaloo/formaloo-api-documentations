Retrieves one lead-enrichment configuration by slug, including its form, source field, provider, and destination mapping.

This is configured integration state, separate from the integration catalog
installation.
