Lists lead-enrichment configurations available in the active workspace.

Each configuration attaches enrichment to a form, identifies the source field used for lookup, and maps provider output keys to saved Formaloo destination field slugs.
