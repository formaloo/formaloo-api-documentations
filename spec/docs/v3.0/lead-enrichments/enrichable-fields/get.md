Lists provider-supported destination keys for lead-enrichment mappings.

Read this catalog before creating or updating an enrichment configuration. Use the returned `key` values in `mapped_fields`; do not guess provider identifiers. Company and people fields are separate because the applicable set depends on the configured source-field type.
