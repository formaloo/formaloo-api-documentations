Lists audiences available through the connected Mailchimp account.

Use the selected audience's returned `id` as `list_id`, then retrieve that audience's merge fields before building `mapped_fields`.
