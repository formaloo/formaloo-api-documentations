Lists merge fields for one Mailchimp audience.

In a form mapping, keys are saved Formaloo field slugs and ordinary merge fields use the returned `tag` and `type`. Additionally map a saved email field with both `tag: email_address` and `type: email_address`. This mandatory email descriptor is separate from the audience's merge-field discovery; `tag: EMAIL` is not a substitute.
