Lists merge fields for one Mailchimp audience.

In a form mapping, keys are saved Formaloo field slugs and values use the returned merge-field `tag` and `type`. At least one mapping must use `type: email_address`.
