Lists WhatsApp content templates in the active workspace with their ContentSid, language, approval status, and any rejection reason.

Use an approved template's `content_sid` as a campaign's `template_id`.
