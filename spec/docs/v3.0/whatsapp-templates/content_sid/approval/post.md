Submits a WhatsApp template for provider approval.

Provide a unique lowercase alphanumeric/underscore `name` and one of the supported categories: `UTILITY`, `MARKETING`, or `AUTHENTICATION`. Submission is a separate lifecycle step from draft creation and approval is asynchronous; poll the approval endpoint for the result.
