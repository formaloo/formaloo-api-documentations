Refreshes and returns the provider approval state for a WhatsApp template.

Inspect `approval_status` and `rejection_reason` before creating a campaign or resubmitting a rejected template. Only approved templates can be used to open WhatsApp conversations.
