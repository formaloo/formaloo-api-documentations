Partially updates an editable WhatsApp template draft by ContentSid.

The language is immutable. Keep numbered placeholders and the `variables` object consistent. After submission, provider approval rules may prevent edits; retrieve approval status before retrying a rejected template.
