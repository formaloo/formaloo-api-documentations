Creates a WhatsApp text-template draft.

Provide a human-readable `friendly_name`, an immutable language code, and a body. Number placeholders sequentially as `{{1}}`, `{{2}}`, and so on; optional default variables use the same numbered keys. Creation does not submit the template for WhatsApp approval.
