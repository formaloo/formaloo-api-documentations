Returns mapping metadata for the `customer`, `vendor`, or `all` NetSuite record selection.

Use returned field names and types in `destination_field`. A NetSuite mapping is grouped under `customer` or `vendor`; each item pairs a saved Formaloo `source_field` slug with discovered destination metadata. Do not guess custom field identifiers.
