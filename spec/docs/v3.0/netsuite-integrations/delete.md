## Behavior

Returns `200` with body `{"data": {"deleted": true|false}}` — not an empty `data` object. `deleted` is `false` if no connection existed to revoke. There is no separate confirmation step.
