Lists current v5 form themes available to the business, including system and custom themes. Use this when rendering theme pickers or choosing styling for a form.

To apply a theme, update the form with the selected theme `slug`:

```json
{
  "theme": "theme-slug"
}
```
