Lists contact attributes from the connected Brevo account. The API path retains Brevo's former Sendinblue name.

Keys in `mapped_fields` are saved Formaloo field slugs; values use the returned attribute `name` and `type`. A runnable mapping includes an `email` or `SMS` attribute.
