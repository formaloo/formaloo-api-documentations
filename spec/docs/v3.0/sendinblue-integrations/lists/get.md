Lists contact lists from the connected Brevo account. The API path retains Brevo's former Sendinblue name.

Use the selected list's integer `id` as the form integration's `list_id`, then retrieve attributes before building the mapping.
