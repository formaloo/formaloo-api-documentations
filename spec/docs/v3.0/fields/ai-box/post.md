# AI box field

This endpoint shows how we can create an AI box field. This field can use an AI box (same as used in ending pages) to allow the user write a custom prompt using the submission data. This prompt will be sent to the AI service upon submission and create a result, only visible to form admin. The end user won't see this field or its result.